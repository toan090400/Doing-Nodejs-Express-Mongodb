<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link
        href="https://fonts.googleapis.com/css?family=Lato:300,300i,700"
        rel="stylesheet"
    />

    <link rel="stylesheet" href="/css/style.css" />
    <link rel="shortcut icon" type="image/png" href="/img/favicon.png" />

    <title>Natours | Exciting tours for adventurous people</title>
</head>
<body>
    <header class="header">
        <nav class="nav nav--tours">
            <a href="/api/v1/views" class="nav__el">All tours</a>
            <form class="nav__search">
            <button class="nav__search-btn">
                <svg>
                <use xlink:href="img/icons.svg#icon-search"></use>
                </svg>
            </button>
            <input
                type="text"
                placeholder="Search tours"
                class="nav__search-input"
            />
            </form>
        </nav>
        <div class="header__logo">
            <img src="/img/logo-white.png" alt="Natours logo" />
        </div>
        <nav class="nav nav--user">
            
            <%

                if(typeof(user) =="undefined" ){%>
                    
                    <a href="/api/v1/views/loginForm" class="nav__el">Log in<a>
                    <button class="nav__el nav__el--cta">Sign up</button>
                <%}
                else if(user){%>
                    <a href="#" class="nav__el">My bookings</a>
                    <a href="/api/v1/users/logout" class="nav__el">Log out</a>
                    <img src="/img/users/user-20.jpg" alt="User photo" class="nav__user-img" />
                    <span><%= user.name %></span>
                    
                <%}
            %>
            <!-- <a href="/api/v1/views/loginForm" class="nav__el">Log in<a></a> -->
            
        </nav>
    </header>